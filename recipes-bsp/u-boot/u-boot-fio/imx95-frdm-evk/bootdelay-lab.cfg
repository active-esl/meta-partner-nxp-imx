# Lab / development: allow breaking into U-Boot over serial (ser2net or ttyACM0).
# NXP imx95 defconfig defaults to bootdelay 0 — too tight on reset loops.
# Production FRDM images may reduce this later; keep interruptible for bring-up.

CONFIG_BOOTDELAY=3
CONFIG_ZERO_BOOTDELAY_CHECK=y
