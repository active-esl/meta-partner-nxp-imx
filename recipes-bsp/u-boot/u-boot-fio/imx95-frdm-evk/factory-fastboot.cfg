# Factory / UUU writes the production U-Boot FIT directly to eMMC user-area
# LBA 0x300.  Do not expose it as FSL FASTBOOT_BOOTLOADER2: that target follows
# the selected boot hardware partition and would overwrite the AHAB container.
# CONFIG_FSL_FASTBOOT_BOOTLOADER2 is not set
