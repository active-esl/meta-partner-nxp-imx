# Compatibility gate for NXP IW612 OpenThread on Foundries LmP v96.
#
# The main product graph already pins the matching NXP connectivity release
# and its narrow Scarthgap compatibility patches.  This derived configuration
# changes only the target so it remains an honest component consumer gate.

header:
  version: 14
  includes:
    - kas/lmp-v96-imx95-frdm-evk-6.12-partner.yml

target: packagegroup-nxp-otbr
